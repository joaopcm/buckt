services:
  postgres:
    image: postgres:17-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: buckt
      POSTGRES_PASSWORD: buckt
      POSTGRES_DB: buckt
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
