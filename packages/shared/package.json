{
  "name": "@buckt/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.7",
    "vitest": "^3"
  },
  "dependencies": {
    "sonare": "^0.0.1",
    "zod": "^4.3.6"
  }
}
